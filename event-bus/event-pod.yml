apiVersion: v1
kind: Pod
metadata:
  name: event-bus
  labels:
    app: event-bus
    rel: stable
spec:
  containers:
    - name: event-bus
      image: event-bus:latest
      ports:
        - containerPort: 4005
      resources: {}
